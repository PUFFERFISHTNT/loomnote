# 00 · 项目总纲（终极定稿 · 项目宪法）

> 本文档是全项目的「宪法」：定位、原则、边界、文档地图与决策记录。任何后续开发与此冲突时，以本文为准（修改需走 06-评审决议 同款流程）。

## 1. 项目身份

- **产品名**：织记 LoomNote（工作名，待最终确认）
- **形态**：Windows + macOS 双端独立桌面应用（当前阶段 = 开发测试版，`pnpm dev` 启动，不打包不签名）
- **一句话定位**：把原始资料交给 AI，产出**能看（多格式渲染）、能演示（动态 HTML）、能考试（交互判分）、能联网（知识图谱）、能成长（记忆自进化）**的活笔记。

## 2. 边界三铁律

1. **独立应用**：自有 UI、自有工程、自有品牌，与 DSH 桌面/插件生态完全解耦；harness 只是被内嵌的底层。
2. **内容主权在本地**：SQLite + 本地资源库为唯一事实源；飞书/Notion 云端仅是同步副本；断网可启可编。
3. **数据零依赖**：记忆、图谱、偏好全部随应用启停，不依赖任何外部常驻服务；AI 模型接入自由（任意 OpenAI 兼容/Anthropic/自定义端点）。

## 2.5 「Harness Agent」定义（织记的 AI 引擎本体）

织记底层是一个**完整的 harness agent，而非模型调用客户端**：

- **能自主行事**：面对任务自主「规划 → 调用工具 → 观察结果 → 修正 → 交付」，多步循环直到完成；
- **能调用工具**：工具注册表管理一切能力（详见 03 §5），**内置联网搜索/网页抓取**、文件读写、OCR 识图、本地知识检索、图谱查询、公式计算等；
- **双协议兼容**：优先用原生 Function Calling（适配火山方舟 Agent Plan 等支持工具调用的端点）；模型不支持时自动降级为**文本标记工具协议**（`:::tool` 指令，容忍解析），任何模型都能当 agent 用；
- **有纪律**：每步有超时、预算与重试；危险工具（删改数据、外部写入、联网外发）必须用户审批；全程事件流可视化（想什么、调了什么工具、结果如何都可见、可打断、可分支）。
- 与 DSH 的关系：**代码同源（MIT 包直接复用），产品独立**——织记通过 npm 依赖复用 DSH 的 agent-loop / Provider / 联网工具等官方包（03 §5），但 UI、插件体系、桌面壳、数据层完全自建，不依赖 DSH 桌面。

## 3. 设计三原则

1. **MD 为底，HTML 为面**：Markdown IR 是唯一事实源（可版本、可迁移、可导出）；展示层由 AI 按知识点需要生成——普通内容排版卡、需演示的知识点生成 HTML5 交互演示。
2. **一切皆可渲染**：md/PDF/Word/Excel/图片/URL 解析进同一 IR，按通道分流渲染（应用三模式 / 飞书 / Notion / 导出），所见同源。
3. **笔记会生长**：新资料不触发整本重写，AI 只做「找接缝 → 出变更集 → 快照合并」；偏好在使用中自蒸馏、自动生效（自进化）。

## 4. 终极文档地图（本系列）

| 编号 | 文档 | 定位 | 状态 |
|---|---|---|---|
| 00 | 项目总纲（本文） | 宪法：身份/边界/原则/决策 | ✅ |
| 01 | 市场与竞品分析 | 全网同类产品调研、评分、机会矩阵 | ✅（子 Agent 调研 + 汇总） |
| 02 | 产品定义与功能规划 | 七大视图、功能对标、AI 增强维度 | ✅ |
| 03 | 技术架构与工程设计 | 选型、数据模型、渲染/AI/图谱/复习、可靠性 | ✅ |
| 04 | 记忆引擎与智能进化 | VCP 源码级调研结论与织记实现方案 | ✅ |
| 05 | 开发执行计划 | 里程碑、验收标准、风险登记、待确认事项 | ✅ |
| 06 | 专家评审与优化决议 | 三路评审打分、讨论结论、终局决策 Delta | ✅（子 Agent 评审 + 决议） |
| 07 | 安全基线 | Electron 三支柱、演示沙箱三件套、密钥与 IPC 纪律、CI 门禁 | ✅ |

过程稿（6 份）归档于 `drafts/`，仅供溯源。

## 5. 关键决策记录（ADR）

| # | 决策 | 理由 | 状态 |
|---|---|---|---|
| D1 | Electron（当前）× Tauri 2（评估位） | 生态/渲染/CI 成熟度优先，Velite 效应暂不可证 | 生效 |
| D2 | 模型 Provider 框架，任意 API 可接 | 用户明确要求（对齐 DSH 惯例） | 生效 |
| D3 | 记忆引擎参照 VCP 源码架构自建，零外部服务 | 用户明确要求；本地主权 | 生效 |
| D4 | 数学内核按公式 TS 重写（效果优先） | 算法/公式不受版权限制；代码文本不复制 | 生效 |
| D5 | 实时生成管线（流内回环/渐进渲染/双缓冲） | docs/06 评估 P0 收益 | 生效 |
| D6 | 存储健康自愈（WAL/断言/备份/隔离区） | 笔记应用数据是第一资产 | 生效 |
| D7 | 多人协作/白板/发布分享一律延后 | 产品边界聚焦 | 生效 |
| D8 | AI 引擎 = 完整 harness agent，**复用 DSH 官方 MIT 包**（`@deepseek-ai/dsh-agent-loop` + `dsh-llm-pi-ai` + `@earendil-works/pi-ai` + `dsh-tool-web` 等，npm 依赖锁 0.1.x）+ 织记薄封装层；cordis 微型 ctx 装配；不引入插件体系/UI 包 | 用户定调 harness agent；实证：DSH monorepo 为 MIT/BSD 纯库且 npm 已发布 | 生效（改判：自建 → 复用） |
| D9 | Electron 43.x / Node 24 / pnpm 10 基线（33 已 EOL） | 评审 B1 | 生效 |
| D10 | 演示沙箱三件套 + 安全基线成文（07） | 评审 B2/B7 | 生效 |
| D11 | 向量升级位改备选池 + EmbeddingStore 接口 | 评审 B3 | 生效 |
| D12 | SQLite 五条 PRAGMA + checkpoint 纪律 | 评审 B5 | 生效 |
| D13 | 评测回归集先行 + 量化护栏全套 | 评审 C1/C6 | 生效 |
| D14 | 思路展开面板降级 P+；睡眠计算温习列为差异化特性 | 评审 C | 生效 |
| D15 | harness 复用 DSH MIT 包（已实证 npm 发布）；当前实现为自建薄 loop（03 §5.1 回退方案），接口隔离、可随时换入 dsh-agent-loop | 用户提案 + 实证 | 生效 |
| D16 | 多接口管理：DeepSeek 官方 API 预设居首 + 火山方舟 + GLM 视觉 + Ollama + 自定义端点 | 用户要求 | 生效（2026-09-18） |
| D17 | 私有仓 UNLICENSED；课程交付物（proposal）不入库 | 用户要求 | 生效 |
| D18 | 演示沙箱改用 data: URL + frame-src CSP（srcdoc 继承父 CSP 曾被静默拦截） | 实测根因 | 生效 |

## 6. 当前状态与下一步（2026-09-18）

**开发测试版 M0–M2 已全部完成并实测通过**：61 项测试全绿、四包 TS strict、真实 Electron 冒烟、截图+modlens 审图闭环、CI 双端矩阵绿（macOS + Windows）；代码已推送私有仓 `github.com/PUFFERFISHTNT/loomnote`（UNLICENSED，proposal 等课程交付物不入库）。
下一步：**M3 图谱与版本**（Cytoscape 画布 + 增量合并 Diff + SqliteMemoryStore + 资产世代化），并推进 AILT9019 课程项目（proposal 已转飞书云文档）。

### 7. draft 关键理念 → 落地跟踪（吸收过程稿）

| 理念来源（docs/drafts/） | 落地状态 |
|---|---|
| VCPToolBox 六条借鉴（中间层引擎/文本工具协议/变量占位符/上下文引力/冷热双通道/清单化扩展） | ✅ 双协议工具调用（:::tool 容忍解析 + 原生 tool_calls）；⚡ 注意力包裹（记忆包裹 buildPackage）；⏳ 变量占位符模板引擎（M3）；⏳ 冷热双通道（M4） |
| 记忆系统（分层 L0–L4/三通道召回/蒸馏闭环） | ✅ chunker/dedup/embedder/recall/fold/packager/rules/engine 全部实现+单测；⏳ SqliteMemoryStore 持久化（M3）、蒸馏 UI（M4） |
| 全栈优化（流内回环/渐进渲染/健康自愈/资产世代化/可观测） | ✅ 流内工具回环、实时预览（250ms 防抖）、存储健康自愈（WAL 五条 PRAGMA + quick_check 隔离区 + JSON 兜底）；⏳ 资产世代化、上下文快照审计（M3） |
| 数学内核（EPA/残差金字塔 TS 化） | ✅ 查询特征预分析（logicDepth/entropy/coverage/novelty）；⏳ 全量复刻按实测增益进入（P2） |